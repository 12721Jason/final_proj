# Common dependencies for all projects
numpy==1.26.0
opencv-python==4.10.0.84
torch==2.1.0
torchvision==0.16.0
Pillow==10.4.0

# Dependencies for YOLOv8 training and UI
# ultralytics already includes many of these, but specifying is good practice
ultralytics==8.2.102

# Dependencies for Gradio Web UIs
gradio==5.34.0

# Dependencies for ONNX conversion (optional but good to have)
onnx==1.18.0
onnxruntime==1.22.0
onnxruntime-gpu==1.20.1

# Dependencies for PyTorch Neural Style Transfer project
matplotlib==3.8.1

